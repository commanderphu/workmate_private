# Session Notes — 2026-07-22

## Bugfixes

- **Calendar Timezone**: `_to_utc()` Helper in `calendar_sync_service.py` — normalisiert naive/aware datetimes vor allen Vergleichen
- **Flutter `CardTheme` → `CardThemeData`**: Breaking Change in Flutter 3.44.0

## Features

### Dokumentenverarbeitung
- **10 Dokumenttypen** definiert: invoice, reminder, contract, receipt, tax_document, payslip, insurance, bank_statement, letter, identity_document
- **Contract Handling**: Kündigungsfrist-Extraktion, Reminder-Schedule 30/14/7/1 Tage vor Deadline
- **Task Auto-Creation**: `suggested_task` und `always_suggest`-Flag für contract + identity_document — erstellt Tasks auch ohne `action_required=true`

### Kalender
- **Automatischer Sync** via Celery Beat alle 15 Minuten (`app.tasks.calendar_sync`)

### Paperless-ngx
- **48h Lookback** statt 1h (robuster gegen transiente Fehler)
- **Bidirektionaler Sync** (Workmate → Paperless, Upload) → [GitHub Issue #1](https://github.com/commanderphu/workmate_private/issues/1)

### Einstellungen
- **API URL** in den App-Einstellungen änderbar (SharedPreferences, kein Rebuild nötig)

## Design & Frontend

- **Workmate Design Kit v1.0.0** integriert: `WmColors` Tokens, Inter-Font, Dark Mode Standard
- **Favicon-Set** für Flutter Web: SVG, PNG 16/32, ICO, Apple Touch Icon
- **Android Adaptive Icons** (mipmap-anydpi-v26)
- **Flutter Web Titel**: `workmate_private` → `Workmate`

## Android / Google Play

- **Release-Keystore** erstellt: `frontend/android/app/workmate-release.jks` (Passwort in `~/.credentials/.workmate_keystore_pass`)
- **Signing-Config** in `build.gradle.kts` via `WORKMATE_KEYSTORE_PASS` Env-Variable
- **Google Play** Paketname `xyz.phudevelopement.workmate_private` registriert, App-Bundle hochgeladen
- App auf Handy installiert (Android 16, wireless ADB)

## Infrastruktur

- **BSI-Meldung** (rpcbind, Port 111) auf DMZ-Server (46.225.163.35) behoben: Service deaktiviert, iptables-Regel persistent
- **AnyDesk** auf cisco (Heimserver) deaktiviert

## Abgeschlossen

- Papierloses Büro KW30-Ziel: Paperless-ngx + WorkmateOS Private + Scan-to-Paperless via SMB ✅
